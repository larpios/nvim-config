#!/usr/bin/bash

eval "$(zoxide init bash)"

export VISUAL="/usr/bin/nvim"
export EDITOR="$VISUAL"

alias ls='ls --color'
alias ll='ls --color -lah'
alias vim='nvim'

if [ -f ~/runscripts.sh ]; then
    . ~/runscripts.sh
fi
